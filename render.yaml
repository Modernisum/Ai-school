services:
  - type: web
    name: ai-school-backend
    env: rust
    rootDir: Backend
    plan: free
    buildCommand: cargo build --release
    startCommand: cargo run --release --bin backend
    envVars:
      - key: PORT
        value: "8080"
      - key: CARGO_BUILD_JOBS
        value: "2"
