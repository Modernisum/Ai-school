# 📝 Dynamic Plan: Docker Cleanup + Qdrant Setup for Semantic Indexing

## 📂 1. Meta
- **Plan File:** `.kilo/plans/1778687297741-eager-wizard.md`
- **Primary Executor:** Code Mode (Single Execution Loop)
- **Status:** [ ] Planning | [ ] In Progress | [ ] Completed

## 🔗 2. Required Context
- **Project Rules:** `.kilo/rules/project-rule.md`

## 🎯 3. Objective
Clean all existing Docker containers (6 stopped containers from previous sessions), then pull and run a **Qdrant** container for semantic indexing, exposing its REST API + gRPC ports, and return the connection URL to the user.

## 🗺️ 4. Execution Steps (Sequential)

### Step A — Docker Cleanup
1. **List running containers** (`docker ps -q`) — currently none running, but verify.
2. **Stop any running containers** (`docker stop $(docker ps -q)`) — safety measure.
3. **Remove ALL containers** (`docker rm $(docker ps -aq)`) — remove all 6 exited containers:
   - `e6d8a470511d` (python:3.11-slim)
   - `17a3e163b45d` (python:3.11-slim)
   - `51860247f5ef` (python:3.11-slim)
   - `6e7a4017629b` (pgvector/pgvector:pg16)
   - `a8f4c321d26e` (redis:7-alpine)
   - `039899585ff7` (python:3.11-slim)

### Step B — Qdrant Container Setup
1. **Pull Qdrant image**: `docker pull qdrant/qdrant:latest`
2. **Create named volume** (optional but recommended for persistence): `docker volume create qdrant_storage`
3. **Run Qdrant container**:
   ```
   docker run -d \
     --name qdrant \
     -p 6333:6333 \
     -p 6334:6334 \
     -v qdrant_storage:/qdrant/storage \
     qdrant/qdrant:latest
   ```
   - Port **6333** → REST API (HTTP)
   - Port **6334** → gRPC interface
4. **Verify container is running**: `docker ps --filter name=qdrant`

### Step C — Return URL to User
- REST API URL: `http://localhost:6333`
- gRPC URL: `localhost:6334`
- Dashboard UI: `http://localhost:6333/dashboard`

## 📋 5. Verification
| Check | Command |
|-------|---------|
| Container running | `docker ps --filter name=qdrant --format "{{.ID}} {{.Status}} {{.Ports}}"` |
| REST API reachable | `curl -s http://localhost:6333/healthz` or PowerShell: `Invoke-RestMethod http://localhost:6333/healthz` |
| Collections list | `curl -s http://localhost:6333/collections` |

## 🛡️ 6. Auto-Debug & Final Verification
- **Functional Goal:** A clean Docker environment with a running Qdrant instance on ports 6333/6334.
- **Architecture Goal:** No stale containers remaining. Qdrant data persisted via named volume for reuse across restarts.