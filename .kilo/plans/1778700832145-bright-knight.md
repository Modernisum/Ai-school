# 🔧 Plan: Fix All 7 Responsibility System Gaps

## 📂 Meta
- **Plan File:** `.kilo/plans/1778700832145-bright-knight.md`
- **Status:** [ ] Planning | [ ] In Progress | [ ] Completed
- **Priority:** HIGH — core features already have backend support, only frontend wiring missing

---

## 🎯 Objective
Wire up the 7 incomplete features where backend APIs exist but frontend (Flutter / React) UIs are missing or using hardcoded placeholder data.

---

## 📊 Gap Summary

| # | Gap | File(s) to Modify | Type |
|---|-----|-------------------|------|
| 1 | **Teacher Dashboard** — hardcoded analytics cards (5, 3, ₹12.5K, 65%) | `teacher_dashboard.dart:253-418` | Flutter |
| 2 | **Salary Generation UI** — no screen calls API | Modify `salary_analytics_screen.dart` | Flutter |
| 3 | **Student Fee Breakdown Screen** — no responsibility list in student app | Create screen in `Apps/chatra/` | Flutter (New) |
| 4 | **Space Requirement Vacancy** — vacancy gaps not shown in admin UI | `ResponsibilityPage.jsx` + Infra API | React |
| 5 | **Material ↔ Responsibility Bridge** — `space_material_requirements` not surfaced | `SpacePage.jsx` + `MaterialPage.jsx` | React |
| 6 | **Real-time WebSocket** — frontend does not subscribe to events | `ResponsibilityPage.jsx` + new hook | React |
| 7 | **CSV/PDF Export UI** — RTK hooks exist but no download trigger | `ResponsibilityPage.jsx` + Infra API | React |

---

## 🗺️ Execution Steps

### Gap 1: Teacher Dashboard Hardcoded → Real API
**File:** `Apps/employee/lib/screens/dashboards/teacher_dashboard.dart`
- Make StatefulWidget load overview data via `getTeacherResponsibilityOverview()`
- Replace hardcoded values (5, 3, ₹12.5K, 65%) with API response fields
- Add loading shimmer + error retry fallback

### Gap 2: Salary Generation UI
**File:** `Apps/employee/lib/screens/common/salary_analytics_screen.dart`
- Wire up `generateSalaries(schoolId, month, year)` API call
- Display month/year picker + per-responsibility earnings breakdown
- Show total salary = sum(responsibility monthly_price × space_count)

### Gap 3: Student Fee Breakdown Screen (New)
**New File:** `Apps/chatra/lib/features/responsibility/screens/responsibility_list_screen.dart`
- Header: total monthly fee (sum of all student_fee values)
- List: responsibilities consumed by student, each with fee amount + assigned teachers
- Using existing `getStudentResponsibilities()` + `getFeeBreakdownByResponsibility()` APIs
- Register route in app_router.dart

### Gap 4: Space Requirement Vacancy
**Files:** `frontend/Vidhyam/src/features/infrastructure/` (React)
- Add `useGetSpaceRequirementsQuery` RTK hook
- Create VacancyCard component: Required vs Assigned with color-coded gaps
- Integrate into ResponsibilityDetailView tab

### Gap 5: Material ↔ Responsibility Bridge
**Files:** React SpacePage + MaterialPage
- Fetch `space_material_requirements` in SpacePage details
- Show "Required Materials" panel with shortage alerts
- Add "Spaces needing this material" column in MaterialPage

### Gap 6: WebSocket Integration
**New File:** `frontend/.../hooks/useResponsibilityWS.js`
- Connect to ws://host/ws/{schoolId}
- Listen for responsibility_* events → invalidate RTK cache
- Auto-refresh lists, toast on updates, exponential backoff reconnect

### Gap 7: CSV/PDF Export UI
**Files:** React ResponsibilityHub + infrastructureApi
- CSV: Fetch data, create Blob, trigger browser download
- PDF: Add 4 new RTK hooks for PDF report endpoints
- Add "Export Report" dropdown with date range picker

---

## ✅ Verification Checklist
- [ ] Teacher dashboard numbers match DB
- [ ] Salary generation shows per-responsibility earnings
- [ ] Student sees responsibility list + fee breakdown
- [ ] Admin sees vacancy gaps (Required vs Assigned)
- [ ] Space shows material requirements panel
- [ ] WebSocket auto-refreshes on multi-tab changes
- [ ] CSV downloads with correct content

## 🛡️ Compliance
- All API calls enforce `schoolId` (multi-tenant isolation ✅)
- Uses existing patterns: Flutter stateful widget + React RTK Query ✅
- No new external libraries introduced ✅
