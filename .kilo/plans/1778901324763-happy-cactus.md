# Material Management Enhancement Plan — Implementation Complete ✅

## Current State Assessment

### ✅ Already Built (Pre-Existing)

| Layer | Component | Status |
|-------|-----------|--------|
| **DB** | `materials` table (name, quantity, unit_price, need_unit, extra_unit) | ✅ |
| **DB** | `space_materials` junction (space → material with quantity, unit_price) | ✅ |
| **DB** | `space_material_requirements` (required_count per space per material) | ✅ |
| **DB** | `material_locations` (space/material/item location tracking) | ✅ |
| **DB** | `material_history` (buy/sell/transfer audit trail) | ✅ |
| **DB** | `notifications` table (full notification system) | ✅ |
| **DB** | `tasks` table (pending tasks) | ✅ |
| **Backend** | Material CRUD + Buy/Sell + Bulk Import (`/resources/materials`) | ✅ |
| **Backend** | Space CRUD with categories (`/resources/spaces`) | ✅ |
| **Backend** | Assign/Get space materials | ✅ |
| **Backend** | Transfer material between spaces (`transfer_space_material`) | ✅ |
| **Backend** | Clone space (copies requirements) | ✅ |
| **Backend** | `get_space_materials` — returns materials with deficit/full/unset status | ✅ |
| **Backend** | `get_materials_dashboard` — total investment, shortage counts | ✅ |
| **Backend** | Notification CRUD + WebSocket real-time push | ✅ |
| **Backend** | Task management (list/add/update status) | ✅ |
| **Frontend** | SpacePage — full space management list/create/edit/delete | ✅ |
| **Frontend** | SpaceDashboard — KPI tiles (total spaces, categories, employees, alerts) | ✅ |
| **Frontend** | SpaceDataGrid — filterable/searchable space card grid | ✅ |
| **Frontend** | SpaceDetailModal — 3-tab (materials/responsibilities/consumers) | ✅ |
| **Frontend** | SpaceMaterialTab — per-material progress bars with unit price/₹ | ✅ |
| **Frontend** | TransferMaterialModal — select source/target/quantity for transfer | ✅ |
| **Frontend** | SpaceAlertBanner — critical (red) / warning (amber) banner component | ✅ |
| **Frontend** | MaterialPage — full materials inventory with KPI tiles, buy/sell | ✅ |
| **Frontend** | NotificationBell — real-time WebSocket notification bell with dropdown | ✅ |
| **Frontend** | infrastructureApi.js — all RTK Query endpoints for spaces + materials | ✅ |

### ✅ Newly Implemented (This Session)

| Layer | Component | Status |
|-------|-----------|--------|
| **DB** | `material_alert_log` table (alert deduplication) | ✅ NEW |
| **DB** | `spaces.budget` column (per-space budget tracking) | ✅ NEW |
| **Backend** | `MaterialMonitor` service — shortage detection, alert/task creation | ✅ NEW |
| **Backend** | `GET shortage-summary` — per-space deficit costs with budget calc | ✅ NEW |
| **Backend** | `POST run-shortage-check` — manual alert trigger | ✅ NEW |
| **Backend** | `GET/PUT /spaces/detail/:name/budget` — budget CRUD | ✅ NEW |
| **Backend** | Enhanced `get_space_materials` — returns cost summary + budget | ✅ ENHANCED |
| **Backend** | `GET /spaces/materials/all` — bulk materials across all spaces | ✅ NEW |
| **Backend** | Monitor wired into all 7 mutation hooks (create/update/delete/sell/assign/transfer/clone) | ✅ WIRED |
| **Frontend** | `BudgetIndicator.jsx` — green/yellow/red budget badge | ✅ NEW |
| **Frontend** | `SpaceMaterialTab` — cost summary header with total value, shortfall, budget | ✅ ENHANCED |
| **Frontend** | `SpaceDetailModal` — inline budget editing with SAVE/Cancel | ✅ ENHANCED |
| **Frontend** | `SpaceDataGrid` — budget indicators on each space card | ✅ ENHANCED |
| **Frontend** | `NotificationBell` — MATERIAL_SHORTAGE icon + View Space navigation | ✅ ENHANCED |
| **Frontend** | `FloorPlanView` — 2D drag-and-drop grid with DndContext | ✅ NEW |
| **Frontend** | `SpaceCard` — draggable space card with material count, deficit badge | ✅ NEW |
| **Frontend** | `FloorPlanPage` — data-fetching page wrapper | ✅ NEW |
| **Frontend** | `InfraModule` — `/dashboard/infra/floorplan` route | ✅ NEW |
| **Frontend** | `TransferMaterialModal` — 2-step flow: material picker → transfer form | ✅ ENHANCED |
| **Deps** | `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities` | ✅ INSTALLED |

---

## GAP Analysis — All Resolved ✅

### ✅ GAP 1: Automated Backend Shortage Alerts

**Fixed by:**
- `Backend/src/services/material_monitor.rs` — full shortage detection + alert/task creation
- `Backend/src/routes/materials.rs` — `get_shortage_summary` + `run_shortage_check` endpoints
- Wired into all 7 mutation hooks via `PostgresResourceService`
- `NotificationBell.jsx` renders MATERIAL_SHORTAGE with navigation to infra spaces

### ✅ GAP 2: Cost Valuation View & Budget Mapping

**Fixed by:**
- Migration adds `budget DECIMAL(12,2)` to `spaces` table
- `get_space_materials` route computes summary (totalValue, deficitValue, deficitCount, budget)
- `get_space_budget` + `update_space_budget` routes in `spaces.rs`
- Frontend API hooks: `useGetSpaceBudgetQuery`, `useUpdateSpaceBudgetMutation`

### ✅ GAP 3: Visual 2D Floor Plan with Drag-and-Drop

**Fixed by:**
- `@dnd-kit` libraries installed
- `FloorPlanView.jsx` — DndContext grid layout with DragOverlay
- `SpaceCard.jsx` — uses `useDraggable` + `useDroppable` from @dnd-kit
- `FloorPlanPage.jsx` — fetches spaces + materials via `useGetAllSpacesMaterialsQuery`
- Drop on target space opens TransferMaterialModal with source space pre-selected
- `/dashboard/infra/floorplan` route registered

### ✅ GAP 4: Color-Coded Budget Indicators

**Fixed by:**
- `BudgetIndicator.jsx`: Green (≤70%) / Yellow (70-90%) / Red (>90%)
- Added to `SpaceMaterialTab` summary header, `SpaceDetailModal` header, `SpaceDataGrid` cards

### ✅ GAP 5: Alerts-to-NotificationPipeline Integration

**Fixed by:**
- `MaterialMonitor` creates notifications in `notifications` table with category `MATERIAL_SHORTAGE`
- `NotificationBell.jsx` handles MATERIAL_SHORTAGE category with Box icon + space navigation

---

## Post-Review Fixes Applied

After code review, the following fixes were made:

| Issue | Severity | Fix |
|-------|----------|-----|
| N+1 price query in `get_shortage_summary` | WARNING | Batched all prices with `WHERE name = ANY($2)` |
| Empty `materialsBySpace` in FloorPlanPage | WARNING | Added `GET /spaces/materials/all` bulk endpoint + `useGetAllSpacesMaterialsQuery` |
| 2nd DB connection for budget query | SUGGESTION | Replaced raw SQL with `get_space_details` service call |
| TransferModal null material on drag-drop | SUGGESTION | Added 2-step flow: material picker → transfer form |

---

## Final Files Summary

### New Files (8 planned + 2 added during review = 10)
| File | Purpose |
|------|---------|
| `Backend/migrations/202605160001_material_alerts_and_budget.sql` | `material_alert_log` table + `spaces.budget` column |
| `Backend/src/services/material_monitor.rs` | Shortage detection, alert/task creation, shortage-summary API |
| `frontend/.../components/space/BudgetIndicator.jsx` | Reusable green/yellow/red budget status badge |
| `frontend/.../components/floorplan/FloorPlanView.jsx` | 2D drag-and-drop floor plan with DndContext |
| `frontend/.../components/floorplan/SpaceCard.jsx` | Draggable space card with material count, deficit badge |
| `frontend/.../pages/FloorPlanPage.jsx` | Floor plan page with data fetching |

### Modified Files (14)
| File | Change |
|------|--------|
| `Backend/src/services/mod.rs` | Register `material_monitor` module + Services field |
| `Backend/src/services/resource/mod.rs` | Add `material_monitor` to PostgresResourceService; wire monitor into 7 mutation hooks |
| `Backend/src/services/resource/inventory.rs` | Add `get_all_spaces_materials` bulk query method |
| `Backend/src/routes/materials.rs` | Add `get_shortage_summary`, `run_shortage_check` endpoints |
| `Backend/src/routes/spaces.rs` | Enhanced `get_space_materials` with cost summary; add `get_space_budget`, `update_space_budget`, `get_all_spaces_materials` |
| `Backend/src/domain/resources.rs` | Register all new routes |
| `Backend/src/repository/resource_repo.rs` | Add `budget` + `spaceId` to `get_spaces` and `get_space_details` |
| `Backend/src/services/traits/resource.rs` | Add `get_all_spaces_materials` to trait |
| `frontend/.../api/infrastructureApi.js` | Add shortage summary, budget, all-spaces-materials API hooks |
| `frontend/.../components/space/SpaceMaterialTab.jsx` | Add cost summary header with BudgetIndicator |
| `frontend/.../components/space/SpaceDetailModal.jsx` | Add budget display/edit; summary from API |
| `frontend/.../components/space/SpaceDataGrid.jsx` | Add BudgetIndicator on space cards |
| `frontend/.../components/space/TransferMaterialModal.jsx` | Add 2-step flow: material picker then transfer |
| `frontend/.../components/ui/NotificationBell.jsx` | Add MATERIAL_SHORTAGE icon + space navigation |
| `frontend/.../pages/infra.jsx` | Add FloorPlan tab |
| `frontend/.../pages/InfraModule.jsx` | Add `/dashboard/infra/floorplan` route |
| `frontend/Vidhyam/package.json` | Add @dnd-kit dependencies |
