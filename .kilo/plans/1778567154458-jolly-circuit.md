# AI School Project - Complete Debug & Fix Plan

## Project Status Assessment
**Date:** 2026-05-12
**Status:** Development Phase - Core functionality built, needs debugging & completion
**Platforms:** React Frontend, Rust Backend, Flutter Mobile

---

## 1. CURRENT STATE ANALYSIS

### Backend (Rust/Axum) - PARTIALLY WORKING
**Issues Found:**
- [ ] Cargo compilation fails - dependency version conflicts
- [ ] Need to fix tokio/zerocopy/serde_json version conflicts
- [ ] Some test files exist but coverage is low
- [ ] Database migrations exist but some may be incomplete
- [ ] Pattern compliance: Good - uses repository pattern, proper error handling

### Frontend (React/Vidhyam) - MOSTLY WORKING
**Issues Found:**
- [ ] 17 modified tracked files indicate recent incomplete work
- [ ] `rework-none` folder with special characters needs cleanup
- [ ] Some UI components exist but need testing
- [ ] Pattern compliance: Follows RTK Query pattern correctly

### Flutter Mobile (Chatra + Employee) - NEEDS VERIFICATION
**Issues Found:**
- [ ] android cmdline-tools missing
- [ ] Android license status unknown
- [ ] Both apps have .env.example but no real .env
- [ ] Pattern compliance: Uses BLoC pattern correctly

### SuperAdmin (aischool) - NEEDS REVIEW
- [ ] Separate Next.js app exists at frontend/aischool/
- [ ] Has its own package.json and build configuration
- [ ] Need to verify it's properly integrated

---

## 2. REQUIRED FIXES BY PRIORITY

### P0 - CRITICAL (Must fix first)
1. **Backend Cargo.toml** - Fix dependency version conflicts
2. **Flutter Android setup** - Install cmdline-tools and accept licenses
3. **Environment files** - Create .env files for Flutter apps from .env.example

### P1 - HIGH (Fix before testing)
1. **API contract validation** - Ensure frontend/backend API match
2. **Database migrations** - Verify all migrations work
3. **Test coverage gaps** - Add missing tests for critical paths

### P2 - MEDIUM (Improve quality)
1. **Code cleanup** - Remove rework-none folder
2. **Pattern compliance** - Audit for hardcoded secrets, missing schoolId
3. **Performance** - Optimize database queries

### P3 - LOW (Nice to have)
1. **Documentation** - Add API documentation
2. **UI polish** - Compare screenshots with Figma designs
3. **Accessibility** - Improvements

---

## 3. MULTI-AGENT TASK ASSIGNMENT

### Agent 1: Backend Rust Specialist
**Tasks:**
- [ ] Fix Cargo.toml/Cargo.lock version conflicts
- [ ] Run `cargo check` and `cargo clippy` to fix all errors
- [ ] Verify all repository trait implementations
- [ ] Review and fix middleware (especially RLS/multi-tenancy)
- [ ] Add missing tests for repository methods
- [ ] Run `cargo test` and ensure all tests pass

**Files to work on:**
```
Backend/src/repository/
Backend/src/services/
Backend/src/routes/
Backend/src/middleware/
Backend/Cargo.toml
Backend/Cargo.lock
```

### Agent 2: Frontend React Specialist
**Tasks:**
- [ ] Run `npm test` and fix failing tests
- [ ] Run `npm run lint` and fix lint errors
- [ ] Audit all API calls match backend routes
- [ ] Verify all protected routes have auth checks
- [ ] Review and fix any hardcoded URLs/secrets
- [ ] Optimize re-renders and performance

**Files to work on:**
```
frontend/Vidhyam/src/features/*/
frontend/Vidhyam/src/components/
frontend/Vidhyam/src/app/
```

### Agent 3: Flutter Mobile Specialist
**Tasks:**
- [ ] Run `flutter doctor` and fix all issues
- [ ] Run `flutter analyze` and fix all warnings
- [ ] Create .env files from .env.example
- [ ] Verify API calls in api_service.dart match backend
- [ ] Add missing tests for BLoC and widgets
- [ ] Run `flutter test` and ensure tests pass

**Files to work on:**
```
Apps/chatra/lib/
Apps/employee/lib/
```

### Agent 4: Database & API Contract Specialist
**Tasks:**
- [ ] Review all migration files in chronological order
- [ ] Run pending migrations against test database
- [ ] Create API contract document comparing all endpoints
- [ ] Verify schoolId isolation in all database queries
- [ ] Ensure no raw SQL concatenation exists
- [ ] Add proper indexes if missing

**Files to work on:**
```
Backend/migrations/
Backend/src/repository/query_builder.rs
All API endpoint definitions
```

### Agent 5: Test & QA Specialist
**Tasks:**
- [ ] Create test matrix (what needs testing)
- [ ] Add missing unit tests (target: 80% coverage)
- [ ] Add integration tests for API endpoints
- [ ] Add widget tests for Flutter UI
- [ ] Add component tests for React UI
- [ ] Create load testing script for critical endpoints

**Files to work on:**
```
Backend/tests/
frontend/Vidhyam/src/**/*.test.jsx
Apps/chatra/test/
Apps/employee/test/
```

### Agent 6: UI/UX Specialist (Optional, for visual review)
**Tasks:**
- [ ] Capture screenshots of current UI
- [ ] Compare with Figma designs
- [ ] Identify visual inconsistencies
- [ ] Recommend improvements
- [ ] Create visual regression tests

---

## 4. TESTING COMMANDS

### Backend (Rust)
```bash
# Compile check
cargo check

# Clippy lint
cargo clippy -- -D warnings

# Format check
cargo fmt --check

# Tests with coverage
cargo test
cargo tarpaulin --out Html --output-dir ./coverage/
```

### Frontend (React)
```bash
# Tests
npm test -- --watchAll=false

# Lint
npm run lint

# Type check
npm run typecheck

# Build check
npm run build
```

### Flutter
```bash
# Doctor check
flutter doctor

# Analyze code
flutter analyze

# Format check
flutter format --check

# Tests
flutter test

# Build APK
flutter build apk
```

---

## 5. PATTERN COMPLIANCE CHECKLIST

### Must Follow (from vidhyam-rules.md)
- [ ] No hardcoded secrets - use .env files
- [ ] schoolId in all database queries
- [ ] Auth checks on all protected routes/screens
- [ ] No raw SQL concatenation
- [ ] Use ? operator, not unwrap/expect
- [ ] RTK Query for React API calls
- [ ] Repository pattern for Rust
- [ ] BLoC pattern for Flutter
- [ ] Existing code patterns must be followed

### Code Style
- [ ] Consistent naming conventions
- [ ] Proper error messages
- [ ] No commented-out code
- [ ] Proper documentation for public APIs

---

## 6. ESTIMATED TIMELINE

| Task | Agent | Estimated Time |
|------|-------|---------------|
| Fix backend compilation | Agent 1 | 2-4 hours |
| Fix React tests/lint | Agent 2 | 2-3 hours |
| Fix Flutter setup | Agent 3 | 2-4 hours |
| Database & API audit | Agent 4 | 3-5 hours |
| Add missing tests | Agent 5 | 4-6 hours |
| **Total (parallel)** | **All** | **1-2 days** |

---

## 7. SUCCESS CRITERIA

### Must Pass:
- [ ] Backend: `cargo test` - all tests pass
- [ ] Backend: `cargo clippy` - no errors
- [ ] Backend: `cargo fmt` - properly formatted
- [ ] Frontend: `npm test` - all tests pass
- [ ] Frontend: `npm run lint` - no errors
- [ ] Flutter: `flutter test` - all tests pass
- [ ] Flutter: `flutter analyze` - no errors
- [ ] API contract: All endpoints match across platforms
- [ ] School isolation: schoolId present in all queries
- [ ] No compilation errors in any platform

### Nice to Have:
- [ ] 80% test coverage
- [ ] Visual regression tests
- [ ] Performance benchmarks
- [ ] API documentation

---

## 8. RISK ASSESSMENT

| Risk | Impact | Mitigation |
|------|--------|------------|
| Backend won't compile | HIGH | Fix Cargo dependencies first |
| Flutter Android issues | MEDIUM | Install cmdline-tools, accept licenses |
| API mismatches | HIGH | Cross-platform audit |
| Database schema issues | HIGH | Test migrations first |
| Low test coverage | MEDIUM | Add tests incrementally |

---

## 9. IMMEDIATE ACTIONS (Next 2 Hours)

1. **Agent 1**: Start with `cargo check` to see exact error messages
2. **Agent 3**: Run `flutter doctor` and `flutter doctor --android-licenses`
3. **Agent 2**: Run `npm test` on Vidhyam frontend
4. **Agent 4**: List all migration files and check schema

---

## 10. DELIVERABLES

### Expected Output:
1. All platforms compile/build successfully
2. All tests pass
3. No linting errors
4. API contract document created
5. Test coverage report generated
6. Visual test screenshots captured
7. Debug report in `.kilo/debug/` directory