name: SuperAdmin Portal CI

on:
  pull_request:
    paths:
      - 'frontend/superadmin/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
      - run: npm install
        working-directory: ./frontend/superadmin
      - run: npm run build
        working-directory: ./frontend/superadmin
